package pages;

import org.openqa.selenium.WebDriver;

// Minimal placeholder class - not used by AddToCartWorkflowTest
public class BestBuyCartPage {
    private WebDriver driver;
    
    public BestBuyCartPage(WebDriver driver) {
        this.driver = driver;
    }
} 